# 创建交接

## 本次改动

- 将“共享 bucket 根路径误覆盖”抽象为默认拒绝规则，而不是依赖操作员记忆。
- 将“项目根目录被完整上传”抽象为发布目录卫生检查与大文件显式放行。
- 将“已有 CNAME 被静默改写”改为显式确认参数。
- 修复 `--prefix` 未参与上传 key 和无证书域名误报 HTTPS 的实现问题。

## 研究与证据

- 参考技能：仅检查了当前 `budui-oss-deploy`；外部 catalog 调研因本机缺少 `npx` 可执行环境失败，详见 `reports/prior-art-research.md`。
- 拒绝的方案：仅增加文档提醒。它无法阻止脚本直接覆盖对象，不能作为安全机制。
- 原创贡献（设计优势）：发布前读取对象列表并默认拒绝非空根路径；发布目录与 DNS 改写双重显式授权。
- 已验证优势：本地回归已证明开发文件与非空共享根路径会在写入前被拒绝。
- 缺失证据：未进行独立人工盲评，记为 `missing evidence`。
